server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/compliance_db
    username: postgres
    password: Amoghgk
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: true

  cache:
    type: simple   # ✅ using in-memory cache (no Redis needed)

# 🔐 JWT
jwt:
  secret: mysecretkeymysecretkeymysecretkey123
  expiration: 86400000